sources:
 my_utility:
    sftp_user: my_user
    sftp_password: my_password

sinks:
  my_snowflake_instance:
    account: my_account
    user: my_user
    password: deprecated
    ssh_key: my_ssh_key
    role: my_role
    warehouse: my_warehouse
    database: my_database
    schema: my_schema